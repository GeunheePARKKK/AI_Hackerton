# 변경 이력 (CHANGELOG)

> AI Ship Design Debugger — 개발 진행 기록
> 프로젝트 컨셉: 사람이 설계하고, AI가 검토·분석·해결안을 제안한다 (Human Design → AI Critic → Human Revision)

---

## LLM 프로바이더 추상화 (`이번 커밋`) · 2026-09-14

- 자연어 명령/챗봇/AI 분석이 매번 Claude Code CLI를 콜드스타트해 호출당 15~25초 걸리던 문제 대응
- `.env`에 API 키가 있으면 직접 HTTP 호출로 전환 (우선순위: Gemini → Groq → OpenAI → Claude CLI 폴백)
- Gemini Flash / Groq 무료 키 기준 응답 1~3초 예상, 키 없으면 기존과 동일하게 동작
- `.env.example` 추가 (키 발급처 안내 포함), README에 속도 개선 가이드 추가

## 설계 도우미 챗봇 (`2dfaa57`) · 2026-09-12

- 3D 화면 우하단 💬 플로팅 버튼 → 질문 답변 전용 챗봇 (비전공자 온보딩용)
- 답변 근거: 장비 역할 사전, 배치 상식, 도구 사용법 + **현재 설계 상태·위반 내역·설계 규정 KB·과거 사례**를 프롬프트에 주입
- 멀티턴 대화 지원 (최근 12개 메시지 유지), 읽기 전용 — 설계 수정 요청 시 Copilot/편집 도구 안내
- 신규: `backend/chat.py`, `POST /api/chat`, `llm.py`의 Claude 호출을 텍스트/JSON 헬퍼로 분리

## Properties 패널 (`3b49b3a`) · 2026-09-12

- 객체 선택 시 좌측 사이드바에 속성 편집 패널 표시
- **ID / 이름 변경**: 장비 ID 변경 시 연결된 배관의 from/to 참조 자동 동기화, 중복 ID 차단
- **배관**: 직경(mm) 변경
- **장비/구조물**: 가로·세로·높이(m) 변경 (바닥 기준 중심 유지)
- 모든 속성 변경 즉시 재검사 — 크기 확대로 새 간섭이 생기면 바로 검출됨

## Phase 7 — AI Copilot (`d28f568`) · 2026-09-12

**자연어 설계 명령**
- 우측 패널 AI Copilot 입력창: 한국어 명령 → LLM이 구조화된 작업(ops)으로 변환 → 서버가 씬에 적용 → 자동 재검사
- 지원 작업: 장비 추가/이동/삭제, 웹 프레임 추가, 배관 추가, 배관 직경 변경
- 예: "지름 65mm 청수 배관을 기관실 왼쪽에 추가해줘" → P-105 생성
- 역할 분리 유지: LLM은 op 생성만, 기하 수정은 Python, 안전 검증은 Rule Engine
- 신규 모듈: `backend/commands.py`, 엔드포인트 `POST /api/command`

**전체 자동 수정 (Agent 루프)**
- `POST /api/autofix`: 위반을 심각도(HIGH 우선) 순으로 해결안 생성 → 최적안 적용 → 재검사 반복
- 위반 4건 데모 씬 기준 약 15초에 전부 해결, 해결 불가 건은 "수동 검토 필요"로 표시
- 수정 내역(어떤 위반 → 어떤 조치)을 단계별 리포트로 반환

**Undo / Redo**
- 모든 변경(수동 편집·해결안 적용·자동 수정·AI 명령)이 서버 히스토리 스택에 기록
- 헤더 ↶/↷ 버튼, Ctrl+Z / Ctrl+Y 단축키, 최대 50단계
- 엔드포인트: `POST /api/undo`, `POST /api/redo`

## Resolver 안정화 (`6524d63`, `21e3561`) · 2026-09-12

사용자 편집 씬에서 "자동 해결안을 찾지 못했습니다"가 발생하던 3가지 원인 수정:

1. **겹침 깊이 미계산** — 장비끼리 완전히 겹치면 실측/요구가 0/0으로 보고되어 이동 후보가 5cm로 고정 → 관통 깊이(penetration depth)를 음수 실측값으로 보고하고 그만큼 이동 후보 생성
2. **Z축 이동 미지원** — 객체가 천장/바닥을 벗어난 경우 X/Y 이동만으로는 해결 불가 → Z축 이동 추가 (일반 간섭에서는 "공중부양" 방지 페널티 부여)
3. **여유공간 0 케이스** — 방 높이와 동일한 프레임은 정확히 이탈량만큼만 이동 가능한데, 안전여유 50mm가 강제 가산되어 모든 후보가 과이동 → 여유 없는 딱 맞는 이동량(snug fit) 후보 추가

추가 개선:
- 정확한 기하학적 분리 거리 계산 + 넓은 이동량 스윕(0.2~3.5m)
- **완화 폴백 티어**: 부작용 없는 해결안이 불가능하면, 대상 위반은 해소하되 부작용 최소인 안을 "⚠ 신규 위반 N건 유발" 경고와 함께 제시 (해결안이 항상 표시됨)
- 선분-박스 거리 삼분탐색 반복 100→48회 (약 2배 가속)

## Phase 6 — 인터랙티브 설계 편집 (`c8e983d`) · 2026-09-12

"미리 만들어둔 씬을 보는 도구"에서 "직접 설계하는 도구"로 전환:

- **장비/구조물 드래그 이동**: 클릭 시 3축 이동 기즈모(TransformControls), 0.05m 스냅
- **배관 경로 편집**: 배관 선택 → 노란 경유점 핸들 드래그 / 더블클릭으로 점 추가 / Shift+클릭으로 점 삭제
- **장비 팔레트**: Pump·Heat Exchanger·Generator·Tank·Engine·Web Frame 추가, 배관 직경 지정 추가, 선택 삭제
- **모든 편집 → 자동 재검사**: 위반 목록과 3D 하이라이트가 실시간 갱신
- `PUT /api/scene` (씬 교체+재검사), `POST /api/save` (파일 영속화)
- 검사 규칙이 특정 객체가 아닌 일반 규칙 기반임이 증명됨 (새 객체도 즉시 검사 대상)

## Phase 1~5 — 코어 파이프라인 (`6e1e0ba`) · 2026-09-11

**Phase 1 · 기반**
- FastAPI 백엔드 + Three.js 단일 HTML 프론트엔드 (빌드 없음)
- 가상 기관실 데이터(`engine_room.json`): 장비 5종, 구조물 6종, 배관 4종 + 의도적 설계 오류 4건 내장
- Pydantic 데이터 모델 (Scene/Equipment/Structure/Pipe, AABB box)

**Phase 2 · Detect (결정론적 검출 엔진)**
- `geometry.py`: 선분↔박스 최단거리(삼분탐색), 선분↔선분 최단거리(Ericson RTCD 5.1.9), AABB 거리 — LLM 개입 0%
- `detector.py` Rule Engine 5종 검사: 배관↔구조물, 배관↔장비(연결 장비 제외), 배관↔배관(직경 반영 표면거리), 장비 겹침, 유지보수 공간 + 이후 선체 경계(OUT_OF_BOUNDS) 추가
- 데모 씬 기준 106건 검사 → 심어둔 위반 4건을 수치까지 정확히 검출, 오검출 0

**Phase 3 · Visualize**
- 우측 검증 패널: 검사/통과/위반 통계, 심각도 배지(HIGH/MEDIUM), 실측 vs 요구 수치
- 3D 연동: 위반 객체 빨간 하이라이트, 위반 지점 펄스 마커, 오류 카드 클릭 시 카메라 포커스

**Phase 4 · Recommend (검증된 해결안)**
- `resolver.py`: 배관 구간 수직 jog(엘보 2개 추가) / 장비·구조물 이동 후보 생성
- 핵심 원칙: 모든 후보는 수정된 씬에 Rule Engine 전체 재실행 → "대상 위반 해소 + 신규 위반 0건" 통과분만 제시
- 스코어링(이동량 + 영향도 페널티: 배관 0 < 장비 800 < 구조 3000) → ★추천 자동 선정
- 초록 고스트로 수정 후 미리보기, 적용/되돌리기

**Phase 5 · Explain + Learn (LLM 연동)**
- `llm.py`: Claude Code CLI headless(`claude -p`) 호출 — API 키 불필요(Max 구독 재사용), 실패 시 템플릿 폴백, 응답 캐시
- `knowledge.json`: 설계 규정 4건 + 과거 사례 4건, 태그 매칭 RAG-lite
- AI 분석 리포트: 왜 문제인가 / 예상 영향 / 추천 근거(대안 비교) / 과거 유사 사례 + 근거 출처(규정·사례 ID) 표기
- 프롬프트에 "제공된 수치를 바꾸거나 만들지 말 것" 명시 — 수치는 전부 엔진 출력

---

## 인프라 / 배포 노트

- 로컬 실행: `python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000`
- 외부 공개: Cloudflare quick tunnel (`cloudflared tunnel --url http://localhost:8000`) — 재시작 시 주소 변경됨
- GitHub: https://github.com/GeunheePARKKK/AI_Hackerton
